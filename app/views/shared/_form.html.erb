<%= form_for(Picture.new) do %>
  <%= hidden_field_tag :image_url, locals.images.standard_resolution.url %>
  <%= button_tag image_tag(locals.images.standard_resolution.url), id: "button"%>
<% end %>
