<div class="row top">

<div class="col-md-12 pic"><%= image_tag current_user.image, class: "img-circle", size: "200x200"%></div>
  <div class="name col-md-12"><%= current_user.nickname %>
    <h4>>>>>><<<<<</h4>
</div>


<div class="bio col-md-12">
  <span><h3><%= current_user.user.full_name %></h3> <%= current_user.user.bio %></span><br>
</div>

<div class="col-md-4"></div>
</div>

<div class="row-border col-md-12"></div>
<h1>NEED TO BE ABLE TO ADD IMAGE - PHOTO EDITOR</h1>

<div class="row">
  <div class="layout-profile col-md-12">
    <% @pictures.each do |pic| %>
      <%= image_tag pic.image_url %>
      <%= link_to "X", picture_path(pic), method: :delete, data: {confirm: "sure you want to delete this photo?!"} %>
    <% end %>
  </div>
</div>




<!-- Trigger the modal with a button -->

<!-- Modal -->
 <% @pictures.each do |pic| %>
 <%= link_to image_tag(pic.image_url), "#myModal-#{pic.id}", :data => { :toggle =>"modal", :target => "#myModal-#{pic.id}"} %>
  <!-- <button type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#myModal">Open Modal</button> -->
  <% end %>
            <% @pictures.each do |pic| %>
<div id="myModal-<%= pic.id %>" class="modal fade" role="dialog">
  <div class="modal-dialog">

    <!-- Modal content-->
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Modal Header</h4>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="layout-profile col-md-12">
              <%= image_tag pic.image_url %>
              <%= link_to "X", picture_path(pic), method: :delete, data: {confirm: "sure you want to delete this photo?!"} %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>
